(library (name command) (public_name core.command)
 (libraries base filename_base stdio univ_map)
 (inline_tests (flags -drop-tag no-js -drop-tag 64-bits-only) (modes js))
 (preprocess (pps ppx_jane ppx_optcomp)))
